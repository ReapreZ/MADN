package io

import model.gameComponent.GameInterface

trait GameDaoInterface {
  /*def getById(id: Long): Option[GameInterface]
  def getAll: List[GameInterface]
  def create(game: GameInterface): GameInterface
  def update(game: GameInterface): Option[GameInterface]
  def delete(id: Long): Boolean*/
}