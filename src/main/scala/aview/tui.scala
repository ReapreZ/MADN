package aview

import model.{Mesh, Field, House, Player, Dice}

class Tui {

    /*def processInputStart(input: String) = {
        input match {
            case "m" => mesh
            case "n" => Mesh.mesh(2,2,2)

        }
    }   */
}
